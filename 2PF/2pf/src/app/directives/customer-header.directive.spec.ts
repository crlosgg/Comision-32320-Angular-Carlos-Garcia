import { CustomerHeaderDirective } from './customer-header.directive';

describe('CustomerHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomerHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
